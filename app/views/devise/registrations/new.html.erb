<div class="container my-5">
<h3>ユーザー登録</h3>
<p>☆ユーザー登録をすることで自身で新たに投稿出来るようになります☆</p>

<%= form_with model: @user, url: user_registration_path, local: true do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="row">
    <lavel class="col-lg-3">名前</lavel>
    <div class="col-lg-3">
    <%= f.text_field :name, autofocus: true, autocomplete: "name", placeholder: "（サイト内での表示名）" %></div>
  </div><br>

  <div class="row">
    <label class="col-lg-3">メールアドレス</label>
    <div class="col-lg-3">
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "（@付け忘れ注意）" %></div>
  </div><br>


  <div class="row">
    <label class="col-lg-3">パスワード
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %>文字以上)</em>
    <% end %><br /></label>
    <div class="col-lg-3">
    <%= f.password_field :password, autocomplete: "new-password" %></div>
  </div><br>

   <div class="row">
     <label class="col-lg-3">パスワード（確認用）</label>
     <div class="col-lg-3">
    <%= f.password_field :password_confirmation, autocomplete: "new-password" %></div>
  </div><br>

  <div class="actions">
    <%= f.submit "登録する", class: "btn btn-primary" %>
   </div><br>
<% end %>

<%= render "devise/shared/links" %>

</div>